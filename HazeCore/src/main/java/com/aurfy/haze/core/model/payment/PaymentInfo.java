package com.aurfy.haze.core.model.payment;

import com.aurfy.haze.core.model.TextualIDModel;
import com.aurfy.haze.core.model.txn.BankOrder;
import com.aurfy.haze.core.model.txn.MerchantOrder;

public class PaymentInfo extends TextualIDModel {
	

	
	
/*
	`pay_id`                VARCHAR(36)       NOT NULL,
    `txn_id`                VARCHAR(36)       NOT NULL,
    `txn_type`              TINYINT           NOT NULL,
    `mer_id`                VARCHAR(36)       NOT NULL,
    `sub_mer_id`            VARCHAR(36),
    `mer_order_id`          VARCHAR(128)      NOT NULL,
    `src_amount`            BIGINT            NOT NULL,
    `src_currency`          CHAR(3)           NOT NULL,
    `op_rate_classifier`    TINYINT           NOT NULL,
    `op_amount`             BIGINT,
    `op_currency`           CHAR(3)           NOT NULL,
    `bank_amount`           BIGINT            NOT NULL,
    `bank_currency`         CHAR(3)           NOT NULL,
    `issue_bank`            VARCHAR(32),
    `card_org`              TINYINT           NOT NULL,
    `encrypted_card_no`     VARCHAR(128),
    `mask_card_no`          VARCHAR(32),
    `flow_ref_id`           VARCHAR(36),
    `complete_date`         TIMESTAMP(3),
    `exchange_rate`         DECIMAL(18, 4),
    `remain_amount`         BIGINT            NOT NULL,
    `remain_currency`       CHAR(3)           NOT NULL,
    `ref_pay_id`            VARCHAR(36),
    `delivery_approval_id`  VARCHAR(36),
    `delivery_date`         TIMESTAMP(3),
    `owner_id`              VARCHAR(36),
    `create_date`           TIMESTAMP(3)         NOT NULL,
    `update_date`           TIMESTAMP(3)         NOT NULL,*/
	
	
	
}
